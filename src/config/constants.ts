/**
 * Defines shared constants used across the application configuration.
 */

// Duration for caching port detection results, in milliseconds.
export const PORT_DETECTION_CACHE_DURATION_MS = 30000; // 30 seconds

// Duration for caching the entire environment configuration, in milliseconds.
export const ENV_CONFIG_CACHE_DURATION_MS = 60000; // 1 minute